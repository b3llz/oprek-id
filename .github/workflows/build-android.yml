
name: Build Android Release (AAB)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.2'
          channel: 'stable'

      # --- INI OBATNYA BOSKU ---
      # Kita suruh robot bikin folder/rangka Android dengan ID yang benar (id.oprek.app)
      - name: Generate Android Chassis
        run: flutter create --org id.oprek --platforms=android .

      - name: Get Dependencies
        run: flutter pub get

      - name: Build AAB
        run: flutter build appbundle --release

      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: oprek-id-release-aab
          path: build/app/outputs/bundle/release/app-release.aab


